//Funzione per sapere se il numero è intero
function isInt(x) {
    //se x in modulo 1 equivale a 0 allora è un numero intero
    if (x % 1 === 0){
        return true;
    }else {
        return false;
    }
}

